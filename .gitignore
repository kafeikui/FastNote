# Dependencies
node_modules

# Build output
dist
dist-electron
release
*.tsbuildinfo

# Local/machine-specific config — never share proxy settings, local secrets, etc.
*.local
.env
.env.*
!.env.example
!*.env.example
.npmrc

# Capacitor: built web assets copied into the native Android project (regenerated by `cap sync`)
apps/mobile/android/app/src/main/assets/public
apps/mobile/android/app/src/main/assets/capacitor.config.json
apps/mobile/android/app/src/main/assets/capacitor.plugins.json

# Self-hosted relay runtime data (usernames + ciphertext blobs).
# Even though everything inside is E2E-encrypted, this is real local/user
# data and must never be pushed to a shared or public remote.
server/data/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.log
